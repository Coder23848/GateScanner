namespace GateScanner
{
    public struct GateScannerSettings
    {
        public bool NoScanner = false;
        public bool BrokenTransmitter = false;

        public GateScannerSettings()
        {
        }
    }
}
